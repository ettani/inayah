<!-- Header Start -->
<header class="position-absolute w-100 top0">
    <div class="container-fluid">





        <div class="top-header d-sm-flex justify-content-between align-items-center position-absolute">
            <ul class="nav social d-md-flex">
                <li><a href="https://instagram.com/associationinayah?igshid=xsya6m1ad4dn" target="_blank"><i class="fab fa-instagram"></i></a></li>
                <li><a href="https://www.facebook.com/associationinayah/"><i class="fab fa-facebook-f"></i></a></li>
                <li><a href="#"><i class="fab fa-snapchat-ghost"></i></a></li>
            </ul>
        </div>

        <nav class="navbar navbar-expand-md navbar-light">
            {% if app.request.get('_route') is same as 'identity_update' %}
            <a class="navbar-brand" href="{{ path('default_index') }}"><img src="{{ asset('uploads/images/' ~ identity.logo.filename) }}" alt="{{ identity.name }}"></a>
            {% else %}
            <a class="navbar-brand" href="{{ path('default_index') }}"><img src="{{ asset('uploads/images/' ~ identity.logo) }}" alt="{{ identity.name }}"></a>
            {% endif %}
            <div class="group d-flex align-items-center">
                <button class="navbar-toggler" type="button" data-toggle="collapse"
                        data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse justify-content-end pt-5" id="navbarSupportedContent">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="{{ path('default_index') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('agir_agir') }}">Agir</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('actualite_actualite') }}">Actualité</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('don') }}">Faire un don</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('message-create') }}">Contact</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Administration</a>
                            <ul class="dropdown-1 dropdown-menu bg-dark">
                                <li class="nav-item"><a class="nav-link" href="{{ path('admin_dashboard') }}">Tableau de bord</a></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('post_create') }}">Rédiger un article</a></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('user-resetpassword') }}">modifier mot de passe</a></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('identity_update') }}">Configuration</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a></li>
                            </ul>
                        </li>
                    {% else %}

                        <li class="nav-item dropdown">
                            <a class="nav-link" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Compte</a>
                            <ul class="dropdown-2 dropdown-menu bg-dark">
                                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                    <li class="nav-item"><a class="nav-link" href="{{ path('user-resetpassword') }}">Modifier mot de passe</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a></li>
                                {% else %}
                                    <li class="nav-item"><a class="nav-link" href="{{ path('user-create') }}">Demande d'aide</a></li>
                                    <li class="nav-item"><a class="nav-link" href="{{ path('user-create') }}">Inscription</a></li>
                                <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                                {% endif %}
                            </ul>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </nav>

    </div>
    <div class="messagedefilant">
        <a class="text-decoration-none" href="{{ path('actualite_actualite') }}">{{ identity.banner }}</a>
    </div>

</header>

<!-- Header End -->